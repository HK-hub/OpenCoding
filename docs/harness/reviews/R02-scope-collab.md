# R02 · 协作与平台层缺口补全与深度扩展评审（impl/13 – impl/24）

> 评审轮次：REVIEW ROUND 2（镜头：缺口补全与深度扩展）
> 范围：`impl/13-agent-teams-impl.md` … `impl/24-a2a-gateway-impl.md`（12 份，11442 → 11846 行，净增 +404）
> 上游输入：`reviews/R01-scope-collab.md`（11 项发现，4 项 carry-over 移交本轮）
> 参照：`research/`（9 家竞品 + 交叉对比 + 台账）、`appendix-b-interface-contracts.md`、`.qoder/rules/*`
> 结论：**carry-over 4 项全部修复；12 份文件按镜头 ⑥⑦⑧⑩⑪ 逐项补全；Mermaid 105/105 解析通过；内部 `§` 引用 0 悬空；新增内容全部为「补充/精化」，未删改既有语义与决策编号。**

---

## 一、Carry-over 修复（4 项，before / after）

| # | 位置 | before | after | 依据 |
| --- | --- | --- | --- | --- |
| 1 | `24-a2a-gateway-impl.md` §⑩ | `### 10.3 性能预算` → `### 10.5 安全` 跳号，§10.4 缺失 | 新增 `### 10.4 容量估算`（7 维表：任务吞吐 / 事件量 / SSE 连接 / 游标幂等键 / 审计 / Webhook 队列 / 路由缓存 + 口径说明）；原内联「容量估算」段落入该节并在后续小节号不变的前提下消缝 | 编号连续性；R02 镜头显式要求「容量估算」小节 |
| 2a | `13:4` | `§⑩.8 记录「反驳证据 + 建议修订」` | `§⑩.6`（该文件 §⑩ 仅 10.1–10.6，10.6 即修订建议节） | 唯一邻近目标 |
| 2b | `13:456` | `阈值一律进 TeamProperties（§⑨.4）` | `§⑨.3`（该文件 §⑨ 仅 9.1–9.3） | 同上 |
| 2c | `15:42` | `§⑥.5、§⑦.2` | `§⑥.4`（§⑥ 仅 6.1–6.4，6.4 即熔断与恢复） | 同上 |
| 2d | `20:36` | `§⑥.6、§⑦.2、§⑩.3` | `§⑥.5`（§⑥ 仅 6.1–6.5，6.5 即断线重连） | 同上 |
| 2e | `23:7` / `23:109` | `§⑩.9` ×2（修订建议、性能预算来源） | `§⑩.8`（§⑩ 仅 10.1–10.8） | 同上 |
| 2f | `24:8` | `并在 §⑩.9 汇总` | `§⑩.8`（依赖修订与建议节） | 同上 |
| 3 | `19-persistence-recovery-impl.md:469` | 「统一 `HarnessException` + `ErrorCode`（附录 B §B.11，…）」——§B.11 实为**错误码全表**，不定义异常类 | 改为：异常按层分工 + 「错误模型见附录 B §B.7『错误模型』，错误码全表见 §B.11（权威是错误码而非异常类名）」 | 经核 `appendix-b-interface-contracts.md`：B.7 = 错误模型，B.11 = 错误码全表 |
| 4 | 12 份文件 | 异常类名分裂（19/20/24 用 `HarnessException` 25 处；13/14/15/16/17/18/21/22 用 `BusinessException` 39 处；规则原文缺异常类名契约） | 统一约定并落到各文件（见 §二），每份文件在异常首次出现处补「异常与编码约定」段 | R02 裁决：**内核层 = `HarnessException`（携带 `ErrorCode`）；外壳层 = `BusinessException`（同携带 `ErrorCode`），外壳全局处理器按 `ErrorCode` 映射；禁止裸 `RuntimeException`** |

**约定落地细则（已写入 12 份文件）**：内核与契约侧（`harness-contract` / `harness-kernel`，零框架）统一 `HarnessException`；外壳侧（`harness-platform` / `harness-host`，Spring）统一 `BusinessException`；域异常子类挂靠内核侧（`HookException extends HarnessException`、`PluginException extends HarnessException`、`DependencyUnavailableException extends HarnessException` 携带可重试标记）；错误模型语义统一指向附录 B §B.7。落点：13 §5 编码约束段、14 §5 同、15 §5 契约纪律、16 §5 同、17 §6 异常纪律、18 §6 同、19 §10 纪律段、20 §10 纪律段、21 §5 纪律段、22 §5 纪律段、23 §5.1 纪律段后、24 §5.1 枚举纪律后；内核签名 `@throws`/`throw` 逐一改齐（13×4、14×6、15×4、16×2、21×5、22×3、19/20 外壳侧 3+3 改回 `BusinessException`）。

---

## 二、逐文件深化摘要（净增行数为 11442 → 11846 的分摊）

| 文件 | 净增 | 主要补全内容（镜头 ⑥⑦⑧⑩⑪ + 错误矩阵 + 竞品取舍） |
| --- | --- | --- |
| 13-agent-teams | +45 | §3.2 增「与竞品对照的取舍」（Claude Code / Roo 五元组 / L-038 反证）；§9.1 增 8 行错误矩阵；§7.2 成员状态机补 `FAILED` 与级联取消边 + 6 行「触发/守卫/副作用」矩阵；§8.1 增字段类型约定；§10.1 增容量估算 + **死锁/熔断精确算法（含可证伪判据：双成员环必检出、FP 两轮不变不得检出、回冲后 PAUSED 未复位即缺陷、账本不可用期间零成功结算）**；§10.2 增六级降级阶梯；§11.4 增故障注入命令 |
| 14-task-plan | +25 | §3.2 竞品取舍（TodoWrite vs 持久工作对象树；L-042/047/076 来源标注）；§9.1 错误矩阵 9 行；§7.1 补 `in_review → cancelled` 与取消/超时/失败三路径（触发/守卫/副作用）+ 矩阵 2 行；§8.1 类型约定；§10.2 容量估算；§10.3 六级降级阶梯；§11.4 注入命令 |
| 15-goal-scheduler | +38 | §3.2 竞品取舍（Grok goal_tracker / MiniMax keepSessions / goose recipe）；§6.3 增**错过与重复触发处置矩阵 6 行（宕机错过 / CATCH_UP_ONE / 双实例 / 重放 fireAt / 时钟前跳 / 手动并发）**；§6.4 补主路径；§7.1 补终止/撤销/超时边 + 补全表；§7.2 补 `Queued → Skipped`；§9.1 错误矩阵 10 行；§10.5 六级降级阶梯；§11.5 注入命令 |
| 16-event-bus | +25 | §3.3 竞品取舍（DeepSeek 追加日志 / OpenCode 幂等不变量 / L-051）；§9.1 错误矩阵 9 行；§10.1 六级降级阶梯；§10.4 增**回放隔离硬断言（会话重放写事务计数=0、环境重放工作区哈希不变、`last_seq` 单调、分歧可复现）**；§10.10 增**演进演练门禁（三件套 + ignorable 两分支不得混淆）**；§8.1 类型约定；§11.5 注入命令 |
| 17-hook-engine | +40 | §3.7 竞品取舍（四形态对齐 + 退出码降为兼容层 + 三例反向采纳）；§7.2 补执行迁移补全（Skip/Timeout/取消）；**新增 §7.3 熔断状态机**（closed/open/half-open，按实例计数）；§9.1 错误矩阵 7 行；§10.1 容量估算；§10.2 六级降级阶梯（安全类 fail-closed 收口）；§8.1 类型约定；§11.5 注入命令 |
| 18-plugin-runtime | +29 | §3.7 竞品取舍（OpenCode 同进程反例 / Cordis 回卷 / 完整性校验 + 市场默认关）；§7.1 补 `Installed → Removed`、`Upgrading → Degraded`、失败路径注记；§9.1 错误矩阵 9 行；§10.1 容量估算；§10.2 六级降级阶梯；§8.1 类型约定；§11.5 注入命令 |
| 19-persistence | +40 | §3.9 竞品取舍（L-053 授权删 / L-086 local-lite / DeepSeek 代模型）；§9.1 错误矩阵 9 行；**新增 §7.4 合规删除请求状态机**（REQUESTED→…→PARTIAL→CERTIFIED）+ 超时路径；§10.5 六级降级阶梯；§8.1 类型约定；(`§B.11` 引用见 §一) |
| 20-workspace | +25 | §3.9 竞品取舍（四实现 Provider / L-072 PathUri / 不镜像 / 软配额）；§9.1 错误矩阵 8 行；§7.3 补 `REGISTERED → FAILED` 缺失边（原图该状态无入边）+ 迁移补全表；§10.2 容量估算（含 PTY/后台/连接池配额）；§10.5 六级降级阶梯；§8.1 类型约定；§11.3 注入命令 |
| 21-git-worktree | +27 | §3.9 竞品取舍（N5 行业空白 / L-048/049/062 采纳与自研增量）；§9.1 错误矩阵 9 行；§7.1 补 `LEASED/MERGE_QUEUED → ABANDONED`，§7.2 补 `MERGE_RUNNING → CANCELLED`（tip 未变守卫）+ 补全表；§6.1–6.4 补全「主路径」标签；§10.2 容量估算；§10.6 六级降级阶梯；§8.1 类型约定；§11.6 注入命令 |
| 22-cli-tui | +27 | §3.8 竞品取舍（L-008/056/057）；§6.1–6.4 补「主路径」，§6.5 补四要素（原无正文）；§7.2 修复审批卡片**缺失入边**（`SCOPE_SELECTING → CONFIRMING`）+ 迁移补全；§9.1 错误矩阵 8 行（退出码/错误码双口径）；§10.2 容量估算；§10.6 增六级降级阶梯（含 §10.6 能力矩阵联动）；§8.1 类型约定；§11.7 注入用例 |
| 23-desktop | +27 | §3.7 竞品取舍（DeepSeek 四类 IPC 边界 / 清单单一来源 / 拒离线队列）；§6 五图补「前置条件」标注；§7.3 审批卡片补全（含与内核 `UNAVAILABLE` 裁决的对齐说明）；§9.2 后错误矩阵 9 行；§10.3 容量估算（内存/窗口/帧批/缓存/诊断包）；§10.4 六级降级阶梯；§8.1 类型约定；§11.5 注入用例 |
| 24-a2a-gateway | +56 | §3.9 竞品取舍（仅 Gemini 有 A2A `[E1]`、ACP 六家；主线选型代价）；**§10.2 增 7 行版本协商矩阵**（交集取最高 / 低于兼容位拒 / 未声明默认 / 非语义化拒 / 必需能力缺 → UNSUPPORTED / 可选缺逐条记 / 联邦不交不改投）；§10.4 新增（carry-over 1）；§9.5 后错误矩阵 10 行；§7.1 补 `queued/paused → cancelled/failed`，§7.2 补完整补全表；§10.7 六级降级阶梯；§8.1 类型约定；§11.5 注入命令 |

---

## 三、镜头覆盖矩阵（机械核对结果）

| 文件 | §⑥ 时序图数（≥3） | 四要素齐备（前置/主路径/异常补偿/幂等并发） | §⑦ 实体状态机（触发+守卫+副作用） | §⑧ 类型/约束/索引/Key 工厂/事件 | §⑩ 数字+容量估算+降级阶梯 | §⑪ 命令+故障注入 | 错误矩阵 | 竞品取舍 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 13 | 4 | ✅ | 2 实体 + 矩阵（补 FAILED/取消） | ✅ | ✅（含精确算法） | ✅ | ✅ | ✅ |
| 14 | 4 | ✅ | 2 实体 + 矩阵（补取消/超时/失败） | ✅ | ✅ | ✅ | ✅ | ✅ |
| 15 | 4 | ✅ | 2 实体 + 补全（终止/撤销/超时） | ✅ | ✅ | ✅ | ✅ | ✅ |
| 16 | 4 | ✅ | 2 实体（webhook 含死信/暂停） | ✅ | ✅ | ✅ | ✅ | ✅ |
| 17 | 3 | ✅ | 2 → **3** 实体（新增熔断机） | ✅ | ✅ | ✅ | ✅ | ✅ |
| 18 | 4 | ✅ | 2 实体 + 补全 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 19 | 4 | ✅ | 3 → **4** 实体（新增删除请求机） | ✅ | ✅（崩溃点矩阵 12 行） | ✅ | ✅ | ✅ |
| 20 | 5 | ✅ | 3 实体（补 LOST/FAILED 语义） | ✅ | ✅ | ✅ | ✅ | ✅ |
| 21 | 4 | ✅ | 2 实体 + 补全 | ✅ | ✅（冲突/回收/丢失提交三精确协议） | ✅ | ✅ | ✅ |
| 22 | 5 | ✅ | 2 实体（补缺失入边） | ✅ | ✅ | ✅ | ✅ | ✅ |
| 23 | 5 | ✅ | 3 实体 + 补全 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 24 | 5 | ✅ | 3 实体 + 补全 | ✅ | ✅（含协商矩阵） | ✅ | ✅ | ✅ |

---

## 四、特殊关注项复核（R02 镜头点名的 6 个高风险系统）

1. **13 死锁/预算熔断可证伪化**：新增精确算法——等待图环 + `FP = SHA-256(等待边集合 + 任务与写范围)` 连续 `fingerprint-rounds`（默认 3）轮不变 + 超时兜底三层；给出**反例构造要求**（FP 连续 2 轮不变不得检出；短暂 GC 假阻塞不得误报；回冲后未复位 `PAUSED` 判缺陷；账本不可用期间出现成功结算判缺陷）。
2. **15 错过/重复触发**：新增 6 行处置矩阵，覆盖宕机错过（`NOTIFY_ONLY` / `CATCH_UP_ONE` 封顶 1 次）、双实例同刻（DB 唯一键兜底）、驱动器重放（`scheduleFired` 快路径）、时钟前跳（禁止连续补跑）、手动与自动键域隔离。
3. **16 事件 Schema 演进 + 回放隔离**：§10.10 增「演进演练门禁」（新版本 golden + 旧读者解析快照 + 受影响消费者组清单；`ignorable` 两分支行为不得混淆）；§10.4 增「回放隔离硬断言」四条（可证伪）。
4. **19 崩溃点矩阵**：既有 12 行矩阵逐行复核（备份/回填/校验/授权删/封口两窗口/孤儿锁/扫描/重放/删除/导入），本轮补 `§7.4` 删除请求状态与「PARTIAL 只重试未完成层、证明未签发不得宣称已删除」。
5. **21 合并队列冲突 + 丢失提交**：§10.3/§10.5 既有精确协议复核通过；本轮补取消路径（`MERGE_RUNNING → CANCELLED` 以「目标分支 tip 未变」为守卫）与丢失提交恢复的故障注入用例（`git-fault-inject --case lost-commit-recover`）。
6. **24 ACP/A2A 版本协商矩阵**：新增 7 行协商判定（取最高共同版本；低于兼容位（当前大版本 − 2）即拒；未声明按最低兼容位；必需能力缺失 → `UNSUPPORTED_CAPABILITY`；联邦不交不改投）。

---

## 五、机械复检（可复核）

- **Mermaid**：`.research-cache/mmd-check/check.mjs`（mermaid 11.17.2 真实解析）→ 12 份 **105/105 通过，0 失败**（本轮新增 2 张状态机：17 熔断、19 删除请求；修改 5 张状态机的边）。
- **内部 `§` 引用**：自研脚本解析 `§①–⑫(.k)` 对同文件标题集合 → **0 悬空**（R01 的 7 处全部修复；未引入新悬空；`§④.1-6` 等为 research/ 外部引用，经核有效）。
- **异常命名**：12 份逐处核对分层归属（内核/契约侧 `HarnessException`，外壳侧 `BusinessException`），无裸抛 `RuntimeException`（唯一 `catch (RuntimeException e)` 位于 18 的崩溃隔离边界，属合法折叠路径）。
- **无占位**：未引入 `TODO` / `FIXME` / `TBD`；未删改任何 `I-*` / `D-*` 决策条目与编号。

---

## 六、剩余问题（未修复项与处置建议）

| # | 问题 | 影响 | 建议 |
| --- | --- | --- | --- |
| 1 | 异常命名约定仅落地本 12 份；impl/01–12、25–35 仍为旧口径（01/03/07… 用 `BusinessException`，12/27/28 用 `HarnessException`） | 全仓追溯与代码生成不一致 | 终局审计统一登记并批量收敛（建议 X-n），本文 §一 的约定可直接复用 |
| 2 | 22（`EXPIRED → UNAVAILABLE` 硬路径）与 23（本地卡片 `EXPIRED → RESOLVED_DENY` + 以内核裁决为准）语义近似但表述不同 | 端行为解释成本 | 已在 23 §7.3 增补对齐说明；如需字段级统一请作者裁决是否把 23 改动为 `UNAVAILABLE` 呈现 |
| 3 | 容量估算落位不统一：24 为独立小节 §10.4；其余 11 份并入既有「性能预算/容量」小节（避免重编号牵连引用） | 机械检索口径不一 | 若要求「每文件独立小节号」，建议在终局编号轮统一（会触发小节重编号，属作者裁决项） |
| 4 | R01 遗留低危项未在本轮处置：F-05（REQ 序号 padding 2 位 vs 不定长）、F-06/07/08（`Sensitivity` 等枚举缺 `code/desc/of()`）、F-11（22/23 域码重划无映射表） | 追溯匹配与枚举自洽 | 仍为作者裁决项；本镜头未扩大范围 |
| 5 | 17/18 使用 `## 1.`–`## 12.` 阿拉伯序号（其余 10 份为 `## ①…⑪`） | 机械脚本需双模式 | 维持现状（R01 已记录范围说明）；若统一编号将牵动全文 `§` 引用，建议不动 |

---

## 七、复核方式（本报告结论可独立复现）

```bash
# Mermaid 全量解析（12 份）
node .research-cache/mmd-check/check.mjs docs/harness/impl/1[3-9]-*.md docs/harness/impl/2[0-4]-*.md
# 内部 § 引用悬空检查（脚本：§①–⑫(.k) 对同文件标题集合，期望 dangling 0）
node /tmp/refcheck2.mjs docs/harness/impl/1[3-9]-*.md docs/harness/impl/2[0-4]-*.md
# 异常命名分层核对（期望：内核/契约侧 Harness、外壳侧 Business；裸抛 RuntimeException 0）
grep -n "HarnessException\|BusinessException\|throw new RuntimeException" docs/harness/impl/1[3-9]-*.md docs/harness/impl/2[0-4]-*.md
```

> 交付说明：本轮改动仅落在 13–24 的 12 份文件与本文；未触碰 12 份以外的任何文件；未新增/删除 `I-*`/`D-*` 决策编号；新增内容均标注为「与竞品对照的取舍 / 错误矩阵 / 容量估算 / 降级阶梯 / 崩溃点与迁移补全」等可机械检索锚点，便于终局审计批量核验。
