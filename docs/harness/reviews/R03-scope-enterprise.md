# R03 · 企业/生态/前沿层评审（impl/25 – impl/35 + 卷 34/35 + 附录 D）

> 评审轮次：REVIEW ROUND 3（镜头：**技术合理性与数字一致性**）
> 范围：`impl/25` … `impl/35`（11 份）+ `34-automation-templates.md` + `35-intelligent-augmentation.md` + `appendix-d-component-inventory.md`；计数同步面：`impl/README.md` §2（仅计数更正）
> 参照基线：`31-capacity-and-cost.md` §2/§4.1/§4.5/§4.7/§10；`24-enterprise-operations.md` §4.3/§4.7/§7/§10；`impl/01–24` 已冻结数字；`reviews/R03-cross-corpus.md` 交接项（N6–N10、D7）
> 结论：**7 项检查全部执行；修复 12 处（1 处引用漂移、3 处数字口径、6 处数字推导/判据窗口、4 处 fail 方向与删除模型补写、1 处层带标注、1 处 README 计数）**；与 `impl/01–24` 的基线数字**未发现矛盾**（唯一口径差异为 impl/25–27 的「5,000 人半规模」子集，已在文内显式声明）；遗留 2 项报告式问题（impl/11 删除模型落点、README REQ 口径与机械复算差）转下一轮。

---

## 〇、方法与工具（可复核）

| # | 手段 | 用途 | 覆盖 |
| --- | --- | --- | --- |
| 1 | `grep -nE '@Service|@Component|@Transactional|org\.springframework|@TableName|Mapper'` 逐文件 + 章节定位（`grep -n '^## \|^### '`） | 层带合规判定：样例是否位于声明「内核契约 / 零框架」的章节内 | 11 份 × 全量命中逐条判类 |
| 2 | 定量抽取（`保留/归档/窗口`、`容量估算`、`性能预算`、`%/ms/GB/TB/万条` 上下文） + 算术复算 | 数字一致性 | 11 份 + 卷 34/35 + 卷 31/24 基线 |
| 3 | `grep -nE 'fail-open|fail-closed|默认关|有意吞|不阻塞主流程'` 逐文件 | fail 方向审计（门 / 上报 / 默认态） | 11 份全量 |
| 4 | 删除模型回引检索（`级联删除|tombstone|DEK 销毁|crypto-shred|删除证明|impl/10 §③`） | 统一删除故事一致性 | 25/26/28/32/35 + impl/10/11 |
| 5 | README §5.3 机械复算全套（`I-` 去重、`REQ-` 定义行、`` ```mermaid ``、围栏奇偶）+ 附录 D 分节行统计（`awk` 行区间） | 计数复核（附录 D / README §2） | 35 份 + 附录 D |

---

## 一、数字一致性表（实测复算，全部可机械复核）

| # | 数字（出处） | 推导/基线 | 结论 |
| --- | --- | --- | --- |
| N1 | 审计 **24–40 万条/日**、热存 14.4GB/30 天、段文件 3.5GB/月、1.2 亿条/年（25 §10.3） | 5,000 人 × 50 轮 ÷ 6 轮/会话 ≈ 4 万会话/日 × (6–10)；`S_e`=1.5KB、压缩比 0.23（卷 31 §2） | ✅ 一致；占卷 31 日事件 3.0×10⁷ 的 0.8–1.3%（≈1%，文内已声明可相加） |
| N2 | `sec.*` 事件 **6 万条/日** = 判定量的 2%（27 §10.1） | 判定量 3.0×10⁶/日（12 工具/轮 × 50 轮 ÷6 轮/会话 ≈ 75 条/会话）；6 万 ÷ 24–40 万 | ✅ 修正后一致（原写「≈20%」→ **15–25%**） |
| N3 | 计量明细 **7×10⁶ 行/日 ≈2.8GB/天**、30 天 ≈84GB（26 §10.3） | 10k × 50 轮 × (2 模型 + 12 工具)；单行 0.4KB | ✅ 一致；与卷 31 事件热存 1.35TB/30 天为并列项（文内已声明相加） |
| N4 | 遥测 **1.8GB/月（典型）/ 56GB/月（上界）**（28 §10.1） | 2.9×10⁴ 批/日 × 2KB × 30；上界撞 64KB/批 | ✅ 修正后一致（补入 580 条/装机/日 ÷ 200 条/批 的推导，消除「60s → 2.9 批/日」的读法歧义） |
| N5 | 制品 **2.2GB/版本**、差分 660MB、2 版本线 ≈5.6GB（28 §10.1） | 180MB × 6 平台 × 2 形态；差分 ≤30% | ✅ 一致 |
| N6 | 自动化 **6,900 次运行/日**、250 万行/年、2,270 万步/年、8.3×10⁵ 事件/日 = 卷 31 日事件的 2.8%（31 §10.3） | 2,000 用户（20% 启用）× 6 实例 × 4 次/周 ÷ 7 | ✅ 一致 |
| N7 | 增强 **6,000 次/日**、成本 **$120–150/日 = 0.22–0.28%**（32 §10.3） | 10k × 20% × 3；单运行 0.8 万 token 按卷 31 §4.2 混价 | ✅ 修正后一致（原「$0.025 → $120」「≈0.2%」自相矛盾，已拆分上界/压降两档） |
| N8 | 实验事件 **4 万/日 ≈1.5×10⁷/年 = 0.13%**；导出 100 万事件 ≤15min（35 §10.1） | 8 活跃实验 × 5,000 事件/日 × 365 | ✅ 一致 |
| N9 | 探针 **1.4 万行/实例/天**、30 天 ≈43 万行（34 §⑩-3） | 5 层 × 86,400s ÷ 30s = 14,400 行/日 | ✅ 一致 |
| N10 | 评测：快照仓 200MB（≈400 会话 × 0.5MB）、核心集 60 任务 ≈120MB、趋势保留 365 天（33 §⑩-3） | 与卷 31 §10「聚合后保留 1 年」对齐 | ✅ 一致 |
| N11 | 留存总表：审计 3 年/1 年/90 天（25）、事件 365/365/30/1095（25↔16）、遥测 30 天/聚合 1 年（28）、转储 14 天（28↔卷 28 §10）、草稿 30 天（32/卷 35） | 卷 24 §10、卷 16、卷 31 §10、卷 28 §10 | ✅ 一致（与 `R03-cross-corpus` N9 同结论） |
| N12 | 预算/水位默认值：三级 50/80/100%、水位 80/90%、动态放宽 ≤30%、回归容差 10%（26、31、32） | 卷 31 §4.5/§4.7 | ✅ 一致（与 `R03-cross-corpus` N10 同结论） |

**与 impl/01–24 的矛盾**：**未发现**。唯一的规模差异是 impl/25 §10.3 显式采用「卷 31 示例租户 10k 的**一半**（5,000 人）」子集口径，impl/27 §10.1、impl/30 §10.3 沿用同源半规模并已互相声明（本轮为 30 §10.3 补注来源，见 §三 修复 F8）。

---

## 二、fail 方向审计（门 / 上报 / 默认态）

| 文件 | 门（Gate） | 上报 / 观测 | 判定 |
| --- | --- | --- | --- |
| 25 | DLP 规则引擎不可用 → 拒绝外发（closed）；审计完整性队列水位超阈切同步写（closed）；break-glass 默认关 | 配额准入 fail-open（宽松放行 + 告警，与 26 §⑩.5 对偶，高风险租户可切 closed） | ✅ 合规（对偶已互引） |
| 26 | `QuotaGate`/`ReservationCounter` fail-closed；审批 UNAVAILABLE → 拒绝 | `UsageRecorder`/事件追加 **有意吞异常 + WARN**（M19） | ✅ 合规（专节 §⑩.5） |
| 27 | KMS/密钥服务不可用 → 敏感写拒绝；解析失败/规则包缺失 → 拒绝或 ESCALATE；凭据代理失败 → 拒绝执行 | 其余降级 + 告警 | ✅ 合规 |
| 28 | 更新四道校验任一失败 → 拒绝应用；egress/禁止项扫描失败 → 丢弃；同意存储不可读 → **视为未同意**；遥测默认三级全关（opt-in） | 许可 fail-open（宽限 30 天，卷 28 §4.6 显式理由） | ✅ 合规（§⑩「闸门 fail 方向」显式段） |
| 29 | 深链验签/时效/nonce 失败 → 拒绝；安装与升级强制签名（closed）；离线缓存仅只读 | 无上报面 | ✅ 合规 |
| 30 | 无门 | 埋点默认关（同意 opt-in）；**本轮补**：上报失败 fail-open + WARN、不阻塞交互（与 28 同口径） | ✅ 已补齐 |
| 31 | 接管通道不可用 → 保持 `BLOCKED` 不放行；市场未签名 → 拒绝装载；**本轮补**：预算闸门判定失败/耗尽 → fail-closed 不启动新运行 | **本轮补**：度量/观测上报 fail-open + WARN（M19） | ✅ 已补齐 |
| 32 | 门禁拦截 → `BLOCKED`；越界写整体拒绝；**本轮补**：预算闸门 fail-closed（`ai.budget.exhausted`） | **本轮补**：计量与观测上报 fail-open + WARN（对偶口径指向 26 §⑩.5） | ✅ 已补齐 |
| 33 | 判官/性能环境不可用 → 显式降级（不静默给分）；**本轮补**：阻塞型门禁（契约/依赖方向/密钥扫描/端点覆盖）判定链不可用 → fail-closed 阻断 | 评测结果上报失败 fail-open + WARN（不影响 CI 判定） | ✅ 已显式化 |
| 34 | 修复工具在 `strength=PARTIAL` → 拒绝执行；生产破坏性步骤双保险拒绝 | 探针失败/证据写入失败 → 不阻断 + WARN；容量控制器不可用 → 不自动扩缩（保守） | ✅ 合规 |
| 35 | 门控 fail-closed（无记录/回源失败/越权 → 关闭，REQ-FRONTIER-04、§⑪.2/§⑪.3） | **本轮补**：指标与事件上报 fail-open + WARN，缺口 >0.1% 才暂停自动判据 | ✅ 已补齐 |

**结论**：11/11 门与上报方向与语料约定一致（预算/权限/审批 = closed；计量/遥测上报 = open + WARN；遥测默认关）；本轮把 5 处「方向未显式声明」（30/31/32/33/35）补齐为显式条款。

---

## 三、修复明细（before → after）

| # | 文件:位置 | before → after | 依据 |
| --- | --- | --- | --- |
| F1 | `impl/25` §3.3 回退触发 + §10.2 性能表 | 「卷 24 §7 的 10ms 预算」→「卷 24 **§4.7（SLO/SLI 表）**」；并注明 §7 仅述「审计写入异步」（`R03-cross-corpus` 交接项 D7） | `24-enterprise-operations.md:223`（§4.7）、`:323`（§7） |
| F2 | `impl/27` §10.1 | 「占其 24–40 万条/日的 ≈ 20%」→「6 万 ÷ 24–40 万 = **15–25%**（中值约 20%）」 | 算术：15%–25% 区间 |
| F3 | `impl/28` §10.1 | 「2.9 批/日」→ 补推导「≈580 条/装机/日 ÷ 200 条/批；60s 为发送窗口上界**非**批次数上界」，并列出 1.8GB / 56GB 月量的算式 | 消除 1440 批/日 的误读 |
| F4 | `impl/32` §10.3 | 「$0.025 → $120/日」「≈0.2%」→ 拆两档：未压降上界 **$150/日**、压降后 **$120/日**；占卷 31 每用户日成本 **0.22–0.28%** | 6,000 × 0.025 / 0.02；÷ $5.4×10⁴ |
| F5 | `impl/32` §⑧.3 | 新增「运行与灰度数据的删除模型」段（级联删除 + 备份 tombstone 重放 + 删除证明；载荷 DEK 加密销毁；灰度桶匿名哈希 → 轮换去关联；`SUPERSEDED` 为逻辑保留） | 统一删除故事（`impl/10` §③ B1）+ 28 §⑧ 遥测口径同构 |
| F6 | `impl/32` §5.1（**层带违规**，取证行 456–501 / 修复后 459–504） | §5.1 标题声明「契约层关键签名（零框架依赖）」，但节内 `RunOrchestrator` 样例含 `import org.springframework...Transactional`（修复后 `impl/32:466`）+ `@Transactional` → 样例前插入**层带标注**：该样例属外壳层 `platform-intel`（Spring 装配与事务边界），非契约层；契约层签名以 `contract/intel` 包为准 | 检查 1「标注为外壳层」处置 |
| F7 | `impl/29` §⑤ | `DeepLinkVerifier`（`@Component` + `StringRedisTemplate`，修复后 `impl/29:417` 前）插入层带标注：属 `harness-platform/platform-eco` 外壳层；契约签名属 `kernel-eco`/`harness-contract` | 同上（29 的 §⑤ 标题含「契约」易误读） |
| F8 | `impl/30` §10.3 | 「5,000 人示例租户口径」→ 明确为「卷 31 示例租户 10k 的**一半规模**」 | 与 25 §10.3 半规模口径对齐 |
| F9 | `impl/30` §10.5 / `impl/31` §10.5 / `impl/33` §⑩-5 / `impl/35` §3.4 | 各补 fail 方向显式条款（见 §二） | 检查 3 |
| F10 | `impl/35` F-03/F-05/F-06/F-09/F-10/F-11 成功判据 | 补「窗口 + 样本」：30 天滚动 ≥50 任务；28 天 ≥1000 次路由；30 天 ≥100 问标注集；首 30 天 ≥20 次导入；30 天 ≥100 条转写；迁移期 + 14 天观察窗（n ≥5） | 检查 6：metric + threshold + **window** 三要素齐备（F-01/02/07/08/12 窗口在成功判据内；F-04 窗口在退出条件内 —— 90 天复用 ≥2 次） |
| F11 | `impl/README.md` §2 | 图数 17：7→**8**、19：9→**10**；合计 296→**298**（补 R03 复算说明，与 `R03-cross-corpus` 的 `impl 298/298 PASS` 一致） | 机械复算 |
| F12 | `impl/31` §11.6.1 / `impl/32` §5.2.1（**前轮代理成果，本轮回验**） | 十二模板三要素矩阵（12 行）/ 八能力三要素矩阵（8 行）逐行具备「可执行验证（断言/用例入口）· 权限天花板 · 失败模式」，行数与名单核对一致，未再改动 | 检查 5（达成） |

---

## 四、机械复核（本轮实跑）

| # | 项 | 结果 |
| --- | --- | --- |
| 1 | `` ```mermaid `` 总数（`impl/*-impl.md`） | **298**（与 README §2 修正后一致；未新增/删除任何图块） |
| 2 | 围栏奇偶（9 份被改文件） | 25=24 / 27=32 / 28=34 / 29=26 / 30=28 / 31=28 / 32=34 / 33=24 / 35=20，**全偶** |
| 3 | `I-` 决策全域去重 | **268**（与 `IMPL-DECISIONS.md` §3.1 一致，未改动任何编号） |
| 4 | 附录 D 分节行数（`awk` 行区间实算） | K=42 / P=33 / E=32 / T=16 / X=14 = **137**，与 §D.7.4 声明一致 |
| 5 | REQ 定义行逐文件复算 | 本轮 11 份与 README §2 口径一致（25/33/34 的 +1 已声明）；**非本域 4 份存在差**（见 §五-2） |
| 6 | 三要素矩阵行数 | 31：12/12；32：8/8 |

---

## 五、遗留问题（报告式，超出本轮修改范围）

1. **`impl/11` 缺统一删除模型落点**：知识库文件的合规删除（`impl/10` §③「级联删除 + 备份 tombstone 重放 + 删除证明」）**无回引、无删除证明条款**（仅有治理/陈旧降权）——`impl/11` 属内核轮范围，本轮未改；建议下一轮按 25/28/32/35 同构补一段并入 `impl/11` §⑧ 或 §⑩。
2. **README §2 REQ 口径与机械复算存在未声明的差**：声明口径（本域定义行 + 已注明的 25/33/34 各 +1）实算应为 **814**，而表内合计 **817**；差 +3 来自未注明的 impl/11(+2)、impl/20(+3)、impl/24(+1) 与 impl/02(−2)。本轮**只更正 Mermaid 计数**（17/19 与合计），REQ 列未动（涉及内核/协作轮自证数），建议由后续轮确认口径后一次性同步。
3. **`R02-scope-enterprise` §五.4 的「27 的 4.8×10⁶ 判定/日」已过时**：现文为 **3.0×10⁶/日**（含完整推导、与 25 §10.3 同源），R02 原文的容量对账建议以本轮 N2/N3 为准。
4. **容量数字仍为设计期工程推算**（除 25/26/31/32/35 已与卷 31 逐项对表外，27/28/29/30/33/34 为新增子域自有口径，卷 31 未覆盖）：建议终局审计时将本轮 §一 表整体登记进容量看板清单（卷 31 §4.7 阈值联动）。
5. **`impl/32` §5.1 的层带标注为文档级处置**（保留样例、加注外壳层）：若后续要求「契约章节只放契约签名」，可将 `RunOrchestrator` 样例整体迁至 §⑩；本轮按「标注」处置（检查 1 允许的两条路径之一）。
