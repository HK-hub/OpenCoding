# R04 · 安全与权限纵深（范围：`impl/05/06/07/08/09/17/18`）

> 执行轮次：Phase B 自审查 · 第 4 轮（lens = **安全与权限纵深 / adversarial**：你是攻击者）。
> 审查范围：`docs/harness/impl/05-tool-system-impl.md`、`06-permission-system-impl.md`、`07-sandbox-executor-impl.md`、`08-skill-system-impl.md`、`09-mcp-gateway-impl.md`、`17-hook-engine-impl.md`、`18-plugin-runtime-impl.md`（7 份）。
> 基准：卷 06（D-PERM-1…12）、卷 07（D-SBOX-1…11）、卷 30（D-SEC-1…12、信任边界 B1–B10）；竞品证据 `research/competitors/{01-claude-code,02-opencode,03-codex,05-minimax-cli}.md`。
> 修改约束：只增不删；中文；决策编号零改动；Java 片段遵守 `.qoder/rules/`；Mermaid 保守写法；`IMPL-DECISIONS.md` 台账未改（修订建议由编排方登记）。
> 行号说明：全部为 R04 取证时点（编辑后）实测行号。
> 结论摘要：**信任边界 33 处**逐条给出阻断控制；**统一管线核对发现 3 处不一致（U1–U3，全部就地修复）**；**绕过登记 20 项**（本轮新增缓解 10 项、其中 9 项为「已修」）；**密钥处理 7/7 文件通过**（补 6 处硬化）；**deny-by-default 3 处偏离已修**（技能写默认、技能公共市场默认、插件安全关键替换）。

## 1. 方法与取证（可复核）

| 步骤 | 手段（仓库根执行） | 输出 |
| --- | --- | --- |
| 越界点清点 | 逐文件通读 + `grep -n "REQ-\|N[1-6] \|HookPoint\|ToolSourceSPI\|PluginSourceSPI"` | 7 文件 33 处越界点（§2） |
| 顺序核对 | `grep -n "十一序\|十二步\|OrderContract\|sandbox.exec.before\|tool.result.before\|脱敏"` 于 05/06/07/17 | 统一管线表（§3）+ 不一致 U1–U3 |
| 绕过扫描 | 对 20 类攻击手法逐条定位控制并回填 | §4 登记表 |
| 密钥核查 | `grep -n "SecretPort\|SecretRedactor\|钥匙串\|明文\|日志" ` 于 7 文件 + 示例逐段目检 | §5（零明文泄漏；6 处补强） |
| 默认值核查 | `grep -n "default\|默认\|:false\|:true"` 于各 §9 配置块与 manifest 默认列 | §6（3 处偏离已修） |
| 格式 | 代码围栏配平：05=20 / 06=26 / 07=30 / 08=30 / 09=28 / 17=24 / 18=24（全偶）；图纸数不变 | 通过 |

## 2. 信任边界表（越界点 → 控制 → 证据）

| # | 文件 | 信任边界（来源 → 目标） | 阻断控制（file:line） | 判定 |
| --- | --- | --- | --- | --- |
| 1 | 05 | 模型输出 → 工具参数 | 严格校验器 + `additionalProperties:false` + 错误回喂不抛异常（05:284–285）；失败阈值升级 | 通过 |
| 2 | 05 | 模型输出 → 工具名（幻觉/伪造） | 定位=注册表反查 + 身份指纹；`STALE_TOOL_CALL` 不执行 handler（05:87、810） | 通过 |
| 3 | 05 | 外部来源（MCP/插件/脚本）→ 注册表 | 三通道同构 + 注册期 Fail-Fast + **名称必须带来源前缀、禁占内置保留名**（05:775，本轮补） | **已修** |
| 4 | 05 | 钩子输出 → 命令执行 | 改写先于鉴权、按最终形态重鉴权（05:68、303）；字段白名单（17:60） | 通过 |
| 5 | 05 | 文件/命令/远端内容 → 模型上下文 | 结果按**不可信内容**回喂并标注来源，注入检测归卷 03（05:810，本轮补）；脱敏先于外置（05:69） | 通过 |
| 6 | 05 | 外置工件 → 回读 | `read_artifact` 再读重新鉴权、不继承首次授权（05:74、168） | 通过 |
| 7 | 06 | 模型/钩子参数 → 权限判定 | 改写后参数构造 `ActionDescriptor`；钩子不可放宽（06:37、274） | 通过 |
| 8 | 06 | 命令文本 → AST 解析 | 歧义即取高（PARTIAL≥R2 / UNPARSED=R4）+ 禁规则沉淀 + 21 条歧义源（06:61、276） | 通过 |
| 9 | 06 | 规则来源（配置/DB/插件/企业服务）→ 求值 | 拒绝优先 + locked 预置 + **层级由宿主按来源绑定、禁自报越级**（06:283、818，本轮补）；**基线同键取高**（06:203，本轮补） | **已修** |
| 10 | 06 | 审批通道（外部应答者）→ 决策 | 应答者身份强校验 + 通道签名 + 终态不可再迁移（06:280） | 通过 |
| 11 | 06 | 审批预览 → 实际执行 | 预览与执行绑定同一 `action_digest`，不一致即拒 + 安全事件（06:284、773，本轮补） | **已修** |
| 12 | 06 | 授权记忆 → 后续决策 | 六级范围 + 三重封顶（不能 DENY→ALLOW）+ 撤销广播（06:279、856） | 通过 |
| 13 | 07 | 子进程 → 宿主文件系统/网络 | 五档隔离 + 围栏 + 路径规范化 + 治理路径首层组件名保护（07:59、845） | 通过 |
| 14 | 07 | 路径校验 → 文件打开（TOCTOU） | **fd + inode 复核（O_NOFOLLOW 语义），禁止 check 与 use 之间跟随链接**（07:846，本轮补） | **已修** |
| 15 | 07 | 凭据哨兵 → 代理替换 → 外部服务 | lease 域名+方法/路径绑定；**跨域重定向剥离凭据**；响应回扫真值（07:561、844，本轮补） | **已修** |
| 16 | 07 | 沙箱助手/围栏参数 → 执行环境 | 运行期见证（`observe` 与期望比对）+ 五道验证 + `partial` 显式上报（07:62、528） | 通过 |
| 17 | 07 | 越界信号 → 处置动作 | 处置梯度（观察→阻断→终止+冻结+告警）+ 逃逸零容忍（07:564 §6.3、07:843 信号面） | 通过 |
| 18 | 08 | 技能内容（正文/资源/脚本）→ 指令与执行 | 能力收窄 N1 + **正文资源按不可信内容注入检测 + 禁 system 覆盖字段**（08:843，本轮补） | **已修** |
| 19 | 08 | 技能清单能力声明 → 授权 | 安装期显式授权 + **未声明即拒绝（写/网络/命令/密钥默认空集）**（08:243、278，本轮补） | **已修** |
| 20 | 08 | 市场/私仓制品 → 本地安装 | 逐文件哈希 + 签名 + 公共市场默认关闭（08:804、840，本轮补） | **已修** |
| 21 | 09 | MCP 服务器 → 工具注册表 | 服务器自报仅作提示（只降不升被拒）；投影名唯一约束（09:251 §4.3 统一出口、09:707 授权表） | 通过 |
| 22 | 09 | 工具名硬串 → 命名空间 | **字符集白名单 + 投影表反查（禁字符串二次切分）+ 内置保留名**（09:837，本轮补） | **已修** |
| 23 | 09 | 服务器 instructions/描述 → 上下文 | **两处注入检测 + 字节上限 + digest 变更告警**（09:838，本轮补） | **已修** |
| 24 | 09 | 远程服务器 → 本地网络/凭据 | 网关强制（禁止静默绕过）；直连经代理并标注 enforcement（09:839，本轮补；09:832） | **已修** |
| 25 | 09 | 外部 MCP 客户端 → 本产品能力 | 一连接一绑定 + 能力=授权交集 + 写类默认不可见（09:58、759） | 通过 |
| 26 | 09 | OAuth 授权服务器 → 令牌 | `SecretPort` 引用存储 + 刷新并发去重 + **撤销端点**（09:56、70、754） | **已修**（撤销端点本轮补） |
| 27 | 17 | 钩子输出 → 工具参数/执行 | N1（无 ALLOW）+ 单向断言 + N2 重鉴权 + `rewrite` 默认关（17:57–59、962） | 通过 |
| 28 | 17 | 钩子改写 → 沙箱策略 | **沙箱策略单向断言**（network 只可收窄、写根只可缩窄）（17:916，本轮补） | **已修** |
| 29 | 17 | 判定型钩子（prompt/agent）→ 会话 | 隔离会话看不到主历史（17:88） | 通过 |
| 30 | 17 | 钩子载荷 → 出网（HTTP/托管） | **出站只允许摘要+引用+digest，`sensitiveAccess` 明文不进载荷**（17:918，本轮补） | **已修** |
| 31 | 18 | 插件代码 → 内核（含反射） | 类加载白名单 + 门面 + **定位诚实声明 + 恶意一律进程外**（18:301、909，本轮补） | **已修** |
| 32 | 18 | 插件清单 → 授权 | 安装期人在场 + 无权限放大 N3 + **授权撤销端点**（18:63、813） | **已修** |
| 33 | 18 | 扩展点实现（安全关键）→ 安全判定 | **`securityCritical` 清单：默认只可叠加收窄、不得替换内置；替换开关默认 false**（18:261、910、866，本轮补） | **已修** |

> 计数口径：33 处 = 05(6) + 06(5) + 07(5) + 08(4) + 09(7) + 17(4) + 18(2 组 4 项合并)；每处均落一名以上控制，未发现「无控制」的裸越界点。

## 3. 统一管线表（权限 × 沙箱 × 钩子，05/06/07/17 四方核对）

```mermaid
flowchart LR
  S1["1 定位解析"] --> S2["2 参数校验"] --> S3["3 tool.call.before 改写"] --> S4["4 权限决策 卷06"] --> S5["5 权限后钩子 仅观察"] --> S6["6 沙箱路由 + sandbox.exec.before"] --> S7["7 执行 卷07"] --> S8["8 结果处理 tool.result.before 掩蔽视图 到 脱敏 到 tool.result.after"] --> S9["9 幂等与账本"] --> S10["10 事件与审计"] --> S11["11 tool.call.after 仅观察"]
```

| # | 阶段（05 主干粒度） | 17 §4.3 全序 | 06 决策链位置 | 07 位置 | 允许的干预 |
| --- | --- | --- | --- | --- | --- |
| 1 | 定位 + 校验 | 1 | — | — | 无钩子 |
| 2 | `tool.call.before` | 2 | 前置 | — | observe/block/rewrite（白名单字段） |
| 3 | 重建 `ActionDescriptor` 重鉴权 | 3 | 入参（06:172 阶段 1） | — | 不可跳过（N2） |
| 4 | 权限决策 | 4 | 九阶段（06:171–180） | 不入沙箱 | 钩子不可放宽；`ask` 不由钩子产生 |
| 5 | `permission.decision.after` | 5 | 出参 | — | **仅 observe** |
| 6 | 沙箱路由 + `sandbox.exec.before` | 6 | — | `select → prepare`（07:417–426） | 策略只能收窄（17:916） |
| 7 | 执行 | 7 | — | `execute`（07:435） | 无钩子（钩子不在隔离器内执行） |
| 8 | 结果处理（掩蔽视图 → 脱敏 → 观察） | 8/9/10 | — | 录制/脱敏（07:844） | 改写仅掩蔽视图；脱敏不可跳过 |
| 9 | 幂等与账本 | 11 | — | — | 无钩子 |
| 10 | 事件与审计 | 11 | 阶段 9 记录 | 先落库后发事件（07:647） | 无钩子 |
| 11 | `tool.call.after` | 12 | — | — | **仅 observe** |

**核对结论：三方相对位置一致**（改写→鉴权→沙箱→执行→脱敏→观察），**无任何路径让钩子或插件在权限后提权**；据此发现并修复 3 处表述不一致：

| # | 不一致 | 处置 |
| --- | --- | --- |
| U1 | 05 称「固定十一序」、17 称「12 步顺序」+ `OrderContract` 断言，两处粒度不同且无映射 | **已修**：05:15 与 05:303 增十二步映射表；17:303 要点④ 反向声明「17 表为权威展开、两表同步修改」 |
| U2 | 05 §3.6.3 步骤 6 未列 `sandbox.exec.before`、步骤 8 未区分 `tool.result.before/after` | **已修**：05:303 映射表逐格补齐（沙箱钩子嵌步骤 6 内部、结果钩子嵌步骤 8 内部） |
| U3 | 结果钩子相对脱敏的位置：卷 17 §4.3「结果类钩子必须在脱敏之后」 vs 卷 05 §4.2 步骤 8「改写→脱敏」；05 未携带 17 §4.7 R1 的裁定 | **已修**：05:303 明确「改写类在脱敏前（仅掩蔽视图）、观察类在脱敏后」，与 17 §4.7 R1 同口径（卷级措辞修订建议维持不改卷） |

## 4. 绕过登记表（bypass | mitigation | file:line | status）

| # | 绕过手法 | 缓解（本轮新增者加 ★） | file:line | 状态 |
| --- | --- | --- | --- | --- |
| B1 | 规则影子 / 层级覆盖（下级放宽上级） | 拒绝优先聚合 + locked 预置 + 越级来源绑定 ★ | 06:281、06:283 | 已缓解 |
| B2 | 解析歧义（编码/嵌套解释器/`base64 -d\|sh`） | 取高 + 禁沉淀 + 21 条歧义源用例 | 06:61、06:276 | 已缓解 |
| B3 | 路径别名穿越（symlink/8.3/UNC/ADS） | canonical+inode 双键 + 首层组件名 + 12 类别名用例 | 06:66、06:274 | 已缓解 |
| B4 | symlink 调包（check 与 use 竞态） | ★ `O_NOFOLLOW` + fd/inode 复核，禁句柄二次按路径解析 | 07:846 | **已修** |
| B5 | 环境变量注入（`env VAR=`、PATH 劫持） | 环境白名单 + `*KEY*/*SECRET*/*TOKEN*` 排除 + 助手绝对路径 | 05:797、07:845 | 已缓解 |
| B6 | 配置弱化判定（改 yml 把高危降 R0） | ★ 基线「同键取高」+ 完整性哈希覆盖基线文件 | 06:203 | **已修** |
| B7 | 钩子提权（改写把 `ls` 变 `rm -rf` / DENY→ALLOW） | 改写先于鉴权 + 单向断言 + `rewrite` 默认关 | 05:68、17:57、17:962 | 已缓解 |
| B8 | 钩子放宽沙箱（网络/写根） | ★ 沙箱策略单向断言（同守卫族、禁止自行解释收窄） | 17:916 | **已修** |
| B9 | 插件替换/停用安全关键扩展点 | ★ `securityCritical` 清单 + 替换开关默认 false + 审批留痕 | 18:261、18:866、18:910 | **已修** |
| B10 | 插件同 JVM 反射逃逸 | ★ 定位诚实声明（防误用非防恶意）+ 恶意走进程外 + 契约对象边界 | 18:301、18:909 | 已缓解（残余 R3） |
| B11 | MCP 工具名伪造/撞内置 | ★ 字符集白名单 + 投影表反查 + 内置保留名 + `uk_projected` | 09:837 | **已修** |
| B12 | 工具描述/instructions 投毒 | ★ 注册与注入两处检测 + digest 变更告警 + 字节上限 | 09:838 | **已修** |
| B13 | MCP 出网绕过代理/凭据明文注入子进程 | ★ 直连经 SandboxProxy（标注 enforcement）+ 凭据哨兵注入 | 09:839、07:59 | **已修** |
| B14 | 凭据搬运 / 开放重定向外泄 | ★ lease 域名+路径绑定、跨域重定向剥离凭据 | 07:561 | **已修** |
| B15 | 技能写范围默认放宽（未声明=工作区根可写） | ★ 写默认空 + 「未声明即拒绝」条款 + 安装期逐项同意 | 08:243、08:278 | **已修** |
| B16 | 技能内容层提权（正文注入/system 覆盖字段） | ★ 注入检测 + `systemPrefix`/`toolSchema` 装载期拒绝 | 08:843 | **已修** |
| B17 | 审批「看 A 跑 B」（预览与执行不一致） | ★ `action_digest` 绑定 + 执行前复核 + 专属安全事件 | 06:284、06:773 | **已修** |
| B18 | 记忆越界 / 无撤销路径 | 六级范围 + 封顶 + 危险操作禁 `always` + 撤销广播 100ms | 06:87、06:279 | 已缓解 |
| B19 | 非交互形态 `ask` 悬挂被误判放行 | `ask` → `deny`（fail-closed）+ 通道不可用即拒 | 06:843、06:78 | 已缓解 |
| B20 | 沙箱降级绕过（配低档/见证 partial 放行） | 降级地板 + `strict-closed` 恒拒 + 见证缺失判 partial | 07:157、07:266 | 已缓解 |

## 5. 密钥处理核查（7/7 通过，补 6 处硬化）

| 文件 | 存储路径 | 日志/事件/示例 | 本轮补强 |
| --- | --- | --- | --- |
| 05 | 敏感字段只存引用名（§8.1）；环境排除模式 | 禁止打印密钥与环境变量值（05:811）；脱敏失败**阻断回喂**（05:829） | 结果不可信回喂标注（05:810） |
| 06 | 审批不含完整参数（仅摘要+引用）；决策存 digest | 禁打印密钥/Token/凭证片段；`explain` 路径相对化（06:856） | 预览纯文本渲染（06:285）；`action_digest` 入表（06:773） |
| 07 | lease 只存 `broker_ref`；CA 句柄引用入 Redis | 日志只打 `leaseId`+域名；录制必过 `SecretRedactor`（07:844） | ★ CA 私钥 A0 仅密钥库引用（07:847）；跨域剥离凭据（07:561） |
| 08 | 凭证只引用不取值；签名/信任根存 ref | 禁写清单敏感字段入日志 | ★ 来源 URL 剥离 userinfo/token（08:845） |
| 09 | `SecretPort` 引用，绝不明文（09:707） | 审计只存 HMAC；URL 隐去 query 令牌 | ★ 本地默认 **OS 钥匙串**（`SecretStoreSPI`）+ 撤销端点（09:707、09:754） |
| 17 | 密钥仅 `SecretPort` 短期句柄 | 只记摘要与 `payload_digest`；输出外溢 TTL 7 天 | ★ 出站载荷禁内联 `sensitiveAccess` 明文（17:918） |
| 18 | 密钥经引用注入；插件只见句柄 | 插件日志按命名空间隔离；配置扫描零明文 | ★ 授权撤销端点（18:813）；`secret` 泄漏延续 §4.6 对账 |

**沙箱网络出口的凭据遮蔽（lens 重点关注）**：`REQ-SBOX-15` + §6.2 时序 + 本轮「替换范围绑定」共同保证——出网真值只在代理内存、按 lease 域名替换、响应回扫、失败宁断不泄（07:86、07:561）。**MCP OAuth 令牌与 provider 凭证**：09 已挂 `SecretPort`→OS 钥匙串默认路径（09:707）；provider 凭证落点在范围外的 `impl/02`，登记为跨文件核对项（§7-H2）。

## 6. deny-by-default 核查（默认 deny/ask，而非 allow）

| 系统 | 偏离项 | 处置 |
| --- | --- | --- |
| 05 | 环境变量、网络、路径、外置回读均为默认拒/白名单；`envKeys` 默认排除敏感键 | 通过 |
| 06 | 超时→deny、无头 `ask`→deny、歧义→ask/deny、`yolo` 默认禁用、危险操作禁 `always`、`max-grant-scope=workspace` 上限 | 通过 |
| 07 | `fail-mode=closed`、地板 R4/R5=L2、`network.mode=allowlist`、`enforcement=advisory` 显式标注 partial | 通过 |
| 08 | **偏离 1**：`writeScopes` 默认=工作区根（allow-by-default）→ **已修**为「写默认空」；**偏离 2**：`allow-public-market` 默认 true 与「trust-root 缺失即启动失败」自相矛盾 → **已修**默认 false 并补 Fail-Fast 条件 | **已修** |
| 09 | `exposure.enabled=false`、`allow-write-tools=false`、sampling/elicitation 默认 ask、网关不可达不直连、组织锁定不可覆盖 | 通过 |
| 17 | `rewrite.enabled=false`、安全类默认 closed、未受信目录拒装、`allow-managed-only=false` 但签名强制 | 通过 |
| 18 | `marketplace.enabled=false`、`trust-root` 启动必填、`reloadable=false`、**偏离 3**：安全关键扩展点可被插件替换且无默认禁令 → **已修**（`security-critical-override=false`） | **已修** |

**「always allow」授权核查**：唯一持久授予面为卷 06 六级记忆——可写/可查/可撤销（`permission.memory.revoke` + Pub/Sub 100ms 一致，06:856 区域）、按范围封顶（06:279）、危险操作禁 `always`（06:87）、每次写入经引擎与审计（`oc_grant_memory.granted_by/source_approval_id`）。插件/技能/MCP 授权分别有 `oc_plugin_grant.revoked_at`、能力撤销路径（08:844）、MCP 撤销端点（09:754、09:759）——**三方均有审计与撤销路径**。

## 7. 残余风险排行（未修 / 需业务裁决）

| 排名 | 风险 | 说明 | 建议归属 |
| --- | --- | --- | --- |
| R1 | L0/L0+ 网络为**约定式**（子进程不强制） | 已用 `enforcement=partial` + `strict⇒R2+强制≥L1` + 事件标注缓解，但个人形态默认档仍可被「不尊重代理变量」的二进制绕过网络白名单 | 卷 07/20（终端提示已就位；可评估 L0+ 也走 netns 的可行性） |
| R2 | 审批预览依赖通道端渲染安全 | 06 本轮要求纯文本渲染 + 控制字符清洗，但 IM 通道（外部平台）渲染不受本产品控制 | 卷 06/22/23（通道适配层验收项，已入 §3.7.6-12 文案） |
| R3 | 进程内插件反射边界 | 本轮已改为诚实声明 + 契约对象边界 + 进程外强制选项；同 JVM 无法证明不可逃逸 | 卷 18/30（企业档强制进程外作为默认建议） |
| R4 | 技能 `eval` 用例与沙箱不可用时的发布门禁 | `block-on-sandbox-unavailable=true` 默认拒绝发布，但离线评测机无隔离器时技能无法发布（可用性张力） | 卷 08/26（需业务裁决简化离线门禁） |
| R5 | 旁路扫描周期（默认 300s；非 JVM 插件 30s） | 事后检测窗口内已发生的未登记副作用不可撤回 | 卷 06/30（结合卷 24 审计告警 SLA） |
| R6 | `sandbox.explain` 与审计的路径信息 | 相对化后仍暴露目录结构，跨租户审计导出需二次脱敏 | 卷 07/24/19（导出链路口径） |

**移交**：H1 = 上表 R1–R6 由编排方分派；H2 = `impl/02`（provider 凭证后端）与 `impl/09` 的 `SecretPort` 后端一致性跨文件核对项；H3 = U3 的卷级措辞修订建议与 17 §4.7 R1 合并登记（不改卷）。

## 8. 本轮文件级变更（只增不删）

| # | 文件 | 变更 |
| --- | --- | --- |
| 1 | `impl/05` | §1.1/§3.6.3 增十二步映射表与顺序不变式；§9.2 名称所有权；§10 安全四条（不可信回喂、fd 复核、denied-read 指针、投影反查） |
| 2 | `impl/06` | SafetyGuard `ESCALATE_ASK` 分支（含 Java 与日志）；基线「同键取高」；具体度次序键；§3.7.6 新增 3 行（来源越级/预览不一致/载荷外泄）；事件 1 项；SPI 约束 2 条；错误矩阵 1 行；`oc_approval.action_digest` |
| 3 | `impl/07` | Q1 表 MCP 行补哨兵；§6.2 替换范围绑定；§10.4 新增 2 条（TOCTOU / CA 与哨兵） |
| 4 | `impl/08` | 写范围默认收口 + 「未声明即拒绝」；公共市场默认关闭 + Fail-Fast；§10.2/§10.3 新增（撤销、正文不可信、URL 脱敏） |
| 5 | `impl/09` | §10.5 重写为 6 条（命名反伪造/描述投毒/出口与凭据/凭证脱敏/撤销/供应链）；`oc_mcp_auth_grant` 钥匙串注；REST 2 个撤销端点；DoD 3 条 |
| 6 | `impl/17` | §4.3 要点④ 映射；§10.3 沙箱单向断言 + 出站边界；DoD 1 条 |
| 7 | `impl/18` | §4.2 `securityCritical` 标注；§4.4 新增门面行；§10.3 两条（定位边界/替换禁令）；配置 1 项；REST 撤销端点；DoD 1 条 |
| 8 | `reviews/R04-scope-security-kernel.md` | 本文件（新建） |

**本轮完成判据自查**：信任边界逐文件枚举（33 处，带控制与 file:line）✅；统一管线表 + 不一致 3 处全部修复 ✅；绕过登记 20 项（含 9 项本轮修复、1 项已缓解残余）✅；密钥处理 7 文件核查 + 6 处硬化（含沙箱出口遮蔽与钥匙串路径）✅；deny-by-default 核查 7 系统、3 处偏离修复 ✅；约束遵守：中文、只增不删、决策编号零改动、Java 片段符合 `.qoder/rules/`、Mermaid 经保守写法（单张 flowchart）✅。
