# R03 · 协作与平台层技术合理性评审（impl/13 – impl/24）

> 评审轮次：REVIEW ROUND 3（镜头：技术合理性与数字一致性）
> 范围：`impl/13-agent-teams-impl.md` … `impl/24-a2a-gateway-impl.md`（12 份）
> 参照基准：`01-harness-architecture.md` §4.5（R1–R5）、`27-technical-path.md` §4.1–§4.2、§4.8.1、`31-capacity-and-cost.md` §4.1–§4.5、Phase A 决策（`DECISIONS.md` D-SCH/D-TEAM/D-GIT/D-A2A 等）、`06-permission-system-impl.md:66/299/810`（审批超时权威值）
> 结论：**依赖方向 5 类标注缺失已补齐（涉 9 处）；并发机制 6 组矛盾已裁决并落地（3 组跨文件 + 3 组文件内/机制混淆）；数字一致性 18 项核对——3 项通过、15 项已修（含 4 项口径声明、3 项跨文件同源、3 项阈值补齐为配置项）；六个高风险算法全部补齐「输入/输出/可证伪断言」；Java 21 合法性修正 5 处（含 1 处 Spring 自调用导致事务失效的真实缺陷）。Mermaid 105/105 通过，Java 代码块括号/花括号 0 失衡。**

---

## 一、方法与可复核口径

1. 机械扫描（全量，非抽样）：`package`/`import`、Spring 与持久化注解、`Mapper`/`Client` 类型、`sealed`/`permits`/`record`、数字声明（EPS/QPS/并发/租约/心跳/重试/保留期/队列深度/预算）。
2. 逐文件读 §5（契约签名）、§⑦（状态机）、§⑩（非功能）与配置表（§⑨），做「文件 × 并发机制」矩阵与「数字 × 基准」对照。
3. 复现命令见 §八（Mermaid 解析 + Java 块括号平衡 + 关键数字检索）。

## 二、镜头 1 · 依赖方向合规（R1–R5：`harness-contract ← harness-kernel ← harness-platform ← harness-host`）

| # | 位置 | 问题 | 处置 |
| --- | --- | --- | --- |
| D-1 | `17-hook-engine-impl.md:46/504` | 内核域落点写 v1 模块名 `open-coding-core-api`（`com.hk.opencoding.core.hook`）+ `open-coding-core-agent`，与其余 10 份的 `harness-contract` / `harness-kernel/kernel-*` 双轨 | 增「模块名对照（迁移期）」段（`17:51`）与包名映射（`17:506`），指向 `27 §4.1/§4.2` |
| D-2 | `18-plugin-runtime-impl.md:49/123/436` | 同上；且 §3.2 的可委派包白名单只写 v1 前缀 `com.hk.opencoding.core.api.*` | 增对照表（`18:55`）、白名单补契约前缀（`18:126`）、签名标题补目标包名（`18:438`） |
| D-3 | `13:857`、`14:852` | 外壳侧写路径示例（`@Service`/`@Transactional`/`Mapper`/`TeamProperties`）无包名与分层标注，易被误读为内核样例（R1 违规） | 补「分层标注」段 + `package com.hk.opencoding.host.app.*`，并显式声明内核/契约侧零 Spring/ORM/Redis |
| D-4 | `21:346-350`、`22:338-342` | 契约包样例 `import lombok.*`，与 `27 §4.1`「零依赖」表述存在张力 | 明确「Lombok = 编译期注解处理、provided 作用域、非运行期依赖，契约/内核允许」（`17:51` 统一声明，`22:338` 就地声明），与仓库既有 `core-api` 实践一致 |
| D-5 | `24:1002` | 验收命令用 v1 模块名 `open-coding-bootstrap` | 就地注记迁移目标 `harness-host/host-bootstrap` |

**结论**：无「Spring/ORM/持久化类型出现在内核或契约样例内」的实质性 R1–R3 违规；`Mapper` 仅出现在平台/外壳样例（`13:864` 认领、`14:861` 迁移、`19:432` 恢复扫描、`20:487` 句柄登记），且事务边界与 `AFTER_COMMIT` 纪律齐备。

## 三、镜头 2 · 并发与一致性矩阵（文件 × 机制）

| 文件 | 资源 | 机制 | 关键参数 | 判定 |
| --- | --- | --- | --- | --- |
| 13 | 黑板/账本/预算 | Redis 短锁 + DB 条件更新 + 部分唯一索引 | `claim-lease-seconds=900`、板锁短临界区 | 通过 |
| 14 | 执行租约 | DB 行租约（部分唯一）+ Redis 心跳 | 900s / 30s、失联 90s | 通过（与 13/21 同规则） |
| 15 | Goal/Schedule 单写者 | `LeasePort`（Redis / 文件锁 / PG advisory） | 30s / 10s；去重键唯一约束兜底 | 通过 |
| 16 | 事件追加与回放 | 分区 seq 行锁串行；回放与追加互斥租约 | 维护锁 600s；消费者幂等 `(group,eventId)` 7 天 | 通过 |
| 17 | 同回合钩子互斥 / 熔断 | Redis 锁 + 回合计数；实例内熔断机 | 30s | 通过 |
| 18 | 装载/重载、远程令牌 | 按插件 Redis 锁、Redis 租约 | 60s；令牌 15min | 通过 |
| 19 | 迁移 / 恢复扫描 | Redis 锁 + DB 状态机 + 副作用账本 | 900s；扫描单例 | 通过 |
| 20 | 连接池 / 断线队列 / 后台句柄 | 心跳 + 有界队列 + 幂等键 + 条件写 | 30s / 重连 5 次；队列 500；**命令队列 256（新增配置项）** | **已修（见 §四 N-9/N-10）** |
| 21 | 合并队列 / worktree | PG 行租约 + 心跳 + Redis 通知；会话内串行通道 | 900s；**心跳 30s、失联 90s（原「租约/3」已改）** | **已修（见 §四 N-6）** |
| 22 | 连接存活 / 审批四值 | 链路探测（5s×3）+ 唯一决定 | 300s 倒计时 | **已修（机制区分，见 §四 N-15）** |
| 23 | 设备在线 / 多窗口订阅 | Redis presence + 作用域去重 | 90s；窗口 ≤ 4 | 通过（连接数已挂 16 总闸） |
| 24 | 入站分区 / 幂等 / 审批 | `callerId` 分区 + 幂等窗口 + 唯一决定 | 窗口 24h；游标 7 天；审批 **5min（已改）** | **已修（见 §四 N-8）** |

**顺序保证一致性核对**：16 §⑩.1「分区内 `seq` 串行、跨分区禁止比较」↔ 14 §①「分区键 = `projectId`，`seq` 单调」+ 水位 `(project_id, view_kind).last_seq`（一个项目 = 一个分区，不自相矛盾）↔ 24 §⑨.1「`Last-Event-ID` = 该任务所属分区的 durable `seq`，禁止拼装全局游标、live 帧不得作游标」（**本轮新增声明**）——三处口径一致；`live` 帧与 `durable` 帧的两族语义在 16 §⑨.1 单源定义，22/23/24 均按该口径引用。

**裁决的三组跨文件矛盾**：① 心跳口径——统一为「间隔 ≤30s（租约类 = `min(租约/10, 30s)`）、失联 = 3 个心跳（≈90s）为上限、成员类可收紧到 2 周期」，改 **21 §⑩.1**（原「租约/3」）并回引 13/14；② worktree 并发——改 **21 §⑩.2**（原「热池 8 / 按需 ≤32」）与 **13 §⑩.1**（每团队 ≤8 受 21 单仓 20 钳制，禁止线性放大）；③ 推送面保活——**16 §⑨.3 新增 `stream.heartbeatSeconds` / `stream.maxConnectionsPerInstance` 作为唯一来源**，22/23/24 改为引用（原仅 24 自定义 500/15s、22 用 5s×3、23 用 90s presence，三套口径无来源）。

## 四、镜头 4 · 数字一致性表（声明 → 基准 → 判定）

| # | 声明位置 | 数值 | 基准 | 判定 |
| --- | --- | --- | --- | --- |
| N-1 | `16:832`（原 830） | 10k 用户单用户 500 事件/日 ≈ 5e6/日 | 31 §4.1：50 轮 × 60 事件 = **3.0×10⁷/日** | **矛盾 → 已修**（含 9×10⁸ 行/月、压缩 10.5 GB/日） |
| N-2 | `16:834`（原 831） | 去重表 = 日事件量 × 7 天 × 5 组 | 3.0×10⁷ 口径下 ≈ 10⁹ 行 | **不可行 → 已修**（订阅面估算 + 分区 + 窗口回退） |
| N-3 | `13:823` | 320 成员会话 | 31 §4.1：≈500 并发活跃会话（NFR-P-9） | **口径缺声明 → 已修**（计入同一口径） |
| N-4 | `13:823` vs `21:789` | 8/团队 × 20 团队 | 21 单实例/单仓上限 20 | **矛盾 → 已修**（受 21 钳制） |
| N-5 | `21:789`（原） vs `21:749` | 热池 8 / 按需 ≤32 | 配置 `hot-pool-size=2`、`max-per-repo=20` | **文件内矛盾 → 已修**（按配置收敛） |
| N-6 | `21:767`（原） | 续租 = 租约/3（300s） | 14 §⑧.2/§⑨.4：900s/30s/失联 90s | **矛盾 → 已修**（统一规则） |
| N-7 | `17:902`（原） | 10 万回合/日 | 31 §4.1：50 万回合/日 | **口径缺声明 → 已修**（子集 + 全量放大 500 万执行/日 ≈ 58 EPS 均值） |
| N-8 | `24:581/840`（原） | 外部审批 10 min | 06 §10：`approval.default-timeout-seconds=300` | **矛盾 → 已修**（默认 5 + 启动校验 ≤ 内核） |
| N-9 | `20:943`（原） vs `20:909` | 16/工作区、64/实例、队列 256 | 配置：每连接 4、pending-op 500 | **矛盾 + 阈值未配置化 → 已修**（4 ⇒ 16/64；新增 `command.queue-capacity=256`） |
| N-10 | `20:943`（原） vs `20:912` | 后台任务 16/会话 | 配置 `background-max-active=8` | **矛盾 → 已修**（引配置值） |
| N-11 | `19:856`（原） | 热 30 天 10 GiB/租户 | 31 §4.1：10k 用户 ≈1.35 TB（含索引 1.8 TB） | **档位歧义 → 已修**（声明小档 + 放大规则） |
| N-12 | `15:867`（原） | 2 万条/日 | 50 Goal × 1440 Tick = 7.2 万行/日 | **算术缺口 → 已修**（声明折叠前提 + 可证伪断言） |
| N-13 | `18:881` | 注册点 ≤2000/租户 | §4.2 目录 23 域/122 类/≥300 点 | **口径歧义 → 已修**（目录容量 ≠ 实注册数） |
| N-14 | `24:915`（原 913） | webhook 重试 5 次 | 16 §9.3：5 次 / 1→60s / 超时 10s | **一致（通过）** |
| N-15 | `22:840` | 5s×3 断线 | 与租约心跳（30s/90s）不同机制 | **混淆风险 → 已修**（机制区分声明） |
| N-16 | `22:819` / `23:853` | 200 客户端 / 4 窗口 | 16 §⑨.3：500 连接/实例 | **相容但未说明 → 已修**（显式挂总闸） |
| N-17 | `15:814`（原 806） | jitter 10%/900s/90s、7 天过期、50 上限 | Phase A REQ-GOAL-14 / D-SCH-1…7 | **一致（通过）** |
| N-18 | `15:812`（原 804） | 默认信封 200 万 token / 50.00 | 31 §4.2：大型任务 ≤$50 | **一致（通过）** |

## 五、镜头 5 · 算法可证伪性清单（逐项：输入/输出 + 反例）

| 文件 | 算法 | 本轮补齐内容 | 反例（可构造 ⇒ 断言非空） |
| --- | --- | --- | --- |
| 13 | 死锁检测 + 预算熔断 | 新增算法接口 `TeamWaitSnapshot → Optional<DeadlockFinding>`（纯函数、幂等）（`13:828`） | FP 连续 2 轮不得检出；回冲后 `PAUSED` 未复位即缺陷；账本不可用期间成功结算即缺陷 |
| 15 | 错过 / 重复触发 | 新增输入 `FireDecision` / 输出 `FireOutcome` + 7 条断言（`15:578`） | `NOTIFY_ONLY` 下补跑即缺陷；双实例产生第二行即缺陷；熔断期新运行即缺陷 |
| 16 | 回放隔离 | 新增四 kind 共用骨架（输入/步骤/输出）+ 5 条断言（`16:852`） | 会话重放出现写事务即缺陷；环境重放工作区哈希变化即缺陷；静默补全区间即缺陷 |
| 19 | 崩溃恢复 | 新增输入/输出 + 6 条断言（`19:890`） | `PENDING` 副作用被判 `REPLAYABLE` 即缺陷；`ABANDONED` 自动改史即缺陷 |
| 21 | 合并队列 | 新增输入 `MergeTask` / 输出 `MergeOutcome` + 6 条断言（`21:816`） | 同目标分支两个 `MERGE_RUNNING` 即缺陷；tip 已变仍回滚即缺陷 |
| 24 | 版本协商 | 新增输入/输出 + 8 条断言（`24:893`） | 交集非空却拒即缺陷；必需能力缺失仍 `ACCEPTED` 即缺陷 |

## 六、镜头 6 · Java 21 合法性修正

1. `13:441-446`：`sealed interface TopologySpec` 原**省略 permits 且 6 个子类型未全列出** → 显式 `permits` 六者 + 注明「同包独立编译单元、禁止跨包实现」。
2. `14:455` / `15:387` / `16:378`：`permits` 引用的类型未在文件内声明（`GoalItem`…、`CronTrigger`…、`SessionCreated`…）→ 补注释说明其为同包独立编译单元 + 禁止别包实现（Java 21 sealed 的同模块/同包约束）。
3. `17:504`：`HookOutcome` 四实现类型明示「同包独立编译单元」。
4. `20:510-570`：修正样例两处真实缺陷——① `protected @Transactional` 方法被同类自调用（Spring 代理失效，事务实际不生效）→ 拆为**独立 Bean** `BackgroundTaskService`；② 进程 spawn（外部副作用）原在事务内且与自身 JavaDoc 矛盾 → 改为「先落库句柄（REGISTERED）→ `AFTER_COMMIT` 启动进程」，并修正 switch 分支返回类型（`CommandResult.ofBackground(...)`）。
5. 全部 12 份 `java` 代码块括号/花括号平衡（脚本扫描 0 失衡）；`List<Foo>~` 仅存在于 Mermaid classDiagram（合法 Mermaid 泛型语法），Java 块内 0 处。

## 七、剩余问题（未修复 / 作者裁决项）

| # | 问题 | 建议 |
| --- | --- | --- |
| 1 | 模块命名双轨仅在本 12 份加「对照段」收敛，impl/01–12、25–35 与 `27 §4.1` 目标名仍需全仓统一（含 `com.hk.opencoding.*` 前缀） | 终局审计统一登记；本轮的对照段可直接复用 |
| 2 | `21:757` 把 preflight 必填判定从启动期移到入队期（依据：仓库级 `.oc/ci.yaml` 启动期不可知），与 `.qoder/rules/config-extraction-rules.md`「必填 Fail-Fast」的字面口径有张力 | 建议在终局审计中以「Fail-Fast 落在可判定点」为规范澄清条目 |
| 3 | 16 新增 `stream.*` 配置后，`22/23/24` 的引用为文档级引用；`.env.example` 模板同步需在实现期落地 | 实现期按配置规范同步模板（本套文档为契约层，未建模板） |
| 4 | `20` 的 `command.queue-capacity` 与 `pty.max-active-per-session` 为本轮新增配置项（原为文本常量） | 已同步写入 `20 §⑨.5`；实现期补 `.env.example` |
| 5 | 24 外部枚举小写 `code` 属线上词表例外，与内部大写约定并存 | 已在 `24:438` 就地声明映射规则；如需全仓枚举大小写规范，建议并入终局规范条目 |

## 八、复核命令（结论可独立复现）

```bash
# Mermaid 全量真实解析（期望 total=105 fail=0）
node .research-cache/mmd-check/check.mjs docs/harness/impl/1[3-9]-*.md docs/harness/impl/2[0-4]-*.md
# Java 代码块括号平衡（期望 0 UNBALANCED / 0 UNCLOSED）
node /tmp/javabalance.mjs docs/harness/impl/1[3-9]-*.md docs/harness/impl/2[0-4]-*.md
# 数字与单源检索（期望：`stream.*` 出现在 16 配置表并被 22/23/24 引用；
# 「租约/3」「热池 8」「单用户日均 500」「默认 10 分钟」仅允许出现在**本轮新增的解释性说明**中，不得作为规则出现）
grep -rn "租约 / 3\|租约/3\|单用户日均 500\|默认 10 分钟\|热池 8" docs/harness/impl/1[3-9]-*.md docs/harness/impl/2[0-4]-*.md
grep -rn "stream.maxConnectionsPerInstance\|stream.heartbeatSeconds" docs/harness/impl/1[3-9]-*.md docs/harness/impl/2[0-4]-*.md
```

> 交付说明：本轮仅改动 12 份 impl 文件与本文；未删除任何小节或决策编号（`I-*` / `D-*` 全部稳定），新增内容均为「对照段 / 输入输出与可证伪断言 / 同源引用 / 配置项补齐」，改动的旧数字均以「收敛到配置或权威基准」方式就地自纠并标注原因。
